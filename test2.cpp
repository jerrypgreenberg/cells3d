#include <iostream>
#include <vector>
#include "Cell.h"
using namespace std;
int main()
{
     std::vector < std::vector <Cell> > AllCells(3,vector<Cell>(0));
/*
     AllCells[0].push_back(1);  
     AllCells[0].push_back(3);  
     AllCells[0].push_back(9);  
     for(vector<int>::iterator i = AllCells[0].begin();i != AllCells[0].end();++i)
     cout << *i <<endl;
*/
}
